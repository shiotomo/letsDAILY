<%= form_for @memo do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_area :body, class: "materialize-textarea" %>

  <%= f.submit "submit", class: "btn green" %>
<% end %>
