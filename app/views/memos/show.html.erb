<h1>Memos#show</h1>
<p>Find me in app/views/memos/show.html.erb</p>

<%= @memo.title %>
<%= simple_format @memo.body %>

<%= link_to 'アップデート', edit_memo_path(@memo) %>
<%= link_to '削除',
  memo_path(@memo),
  method: :delete,
  data: {confirm: '本当に削除していいですか？'}
%>
