<h1>Progress List</h1>
<%= link_to 'tasks/new', new_task_path, class: "btn blue" %>

<% @tasks.each do |task| %>
  <div class="card">
    <div class="card-content">
      <span class="card-content">
        <%= link_to task.title, task_path(task) %>
      </span>
    </div>
  </div>
<% end %>
